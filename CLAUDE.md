# CLAUDE.md

> For Claude Code / AI Agents:
> Read and follow the mandatory architecture and collaboration rules in `AGENTS.md`.
> - Never edit `index.html`, `pong.html`, or `spacewar.html` directly.
> - All changes go into `src/`.
> - Run `node src/test.mjs` and `node src/build.mjs` to build and test.
